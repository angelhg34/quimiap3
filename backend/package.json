{
  "dependencies": {
    "axios": "^1.7.3",
    "paquete": "^1.0.0"
  },
  "scripts": {
    "start-api": "json-server --watch trabajo.json --port 4000"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.2"
  }
}
